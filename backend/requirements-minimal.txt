# Minimal requirements for running the application
# Use this if you have issues with the full requirements.txt

# Core WebSocket and async
websockets>=12.0
python-dotenv>=1.0.0

# Only needed if you plan to add FastAPI server later
# fastapi>=0.109.0
# uvicorn[standard]>=0.27.0
# pydantic>=2.5.0
